// Generated by CoffeeScript 1.9.3
(function() {
  var _, a, addNumbers, limit, listed, sum;

  _ = require("underscore");

  limit = 1000;

  listed = function(cb, cb2) {
    var arr, checkNum, i;
    arr = [];
    i = 0;
    checkNum = function(num, x) {
      if (0 === (x % num)) {
        return true;
      } else {
        return false;
      }
      return x % num;
    };
    while (i < limit) {
      if (checkNum(3, i)) {
        arr.push(i);
        i++;
      } else if (checkNum(5, i)) {
        arr.push(i);
        i++;
      } else {
        i++;
      }
    }
    return cb(arr, cb2);
  };

  sum = function(memo, num) {
    return memo + num;
  };

  addNumbers = function(narr, nfn) {
    var a;
    a = _.reduce(narr, sum, 0);
    return a;
  };

  a = listed(addNumbers, console.log);

  console.log("Program result is: ", a);

}).call(this);
