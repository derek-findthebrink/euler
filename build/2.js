// Generated by CoffeeScript 1.9.3
(function() {
  var _, evenTester, exports, fibonacciSum;

  _ = require('underscore');

  evenTester = function(num) {
    if (0 === num % 2) {
      return true;
    } else {
      return false;
    }
  };

  fibonacciSum = function() {
    var closure, curr, fib, limit, prev, sum, sumArr;
    limit = 4000000;
    curr = 1;
    prev = 0;
    sum = 0;
    fib = [0];
    sumArr = [0];
    closure = function(x, y) {
      fib.push(x);
      if (evenTester(x)) {
        sum += x;
        sumArr.push(x);
      }
      prev = x;
      return curr = y + x;
    };
    while (curr < limit) {
      closure(curr, prev);
    }
    console.log(fib);
    console.log(sumArr);
    return console.log(sum);
  };

  fibonacciSum();

  exports = module.exports = evenTester;

}).call(this);
