// Generated by CoffeeScript 1.9.3
(function() {
  var assert, should;

  should = require('should');

  assert = require('assert');

  describe('Modulo', function() {
    describe("by itself", function() {
      return it("should return 0 when operated on by itself", function() {
        assert.equal(0, 3 % 3);
        assert.equal(0, 5 % 5);
        assert.equal(0, -1 % -1);
        return assert.equal(1, 16 % 5);
      });
    });
    return describe("comparator", function() {
      return it("should compare correctly with 0", function() {
        assert.equal(true, 3 % 3 === 0);
        assert.equal(true, 10 % 5 === 0);
        assert.equal(true, 12 % 3 === 0);
        assert.equal(true, 12 % 4 === 0);
        return assert.equal(false, 4 % 12 === 0);
      });
    });
  });

}).call(this);
